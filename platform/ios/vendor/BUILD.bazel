load("//bazel:flags.bzl", "WARNING_FLAGS")

objc_library(
    name = "SMCalloutView",
    srcs = glob([
        "SMCalloutView/*.m",
    ]),
    hdrs = glob([
        "SMCalloutView/*.h",
    ]),

    includes = ["SMCalloutView"],

    copts = WARNING_FLAGS["ios"] + [
        "-target", "arm64-apple-ios11.0",
        "-isysroot", "__BAZEL_XCODE_SDKROOT__",
        "-F__BAZEL_XCODE_SDKROOT__/System/Library/Frameworks",
    ],

    sdk_frameworks = ["UIKit"],

    platform_type = "ios",   # ✅ CRITICAL FIX

    tags = ["manual"],
    visibility = [
        "//platform:__pkg__",
        "//platform/ios:__pkg__",
    ],
)
