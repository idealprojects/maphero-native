load("//bazel:flags.bzl", "WARNING_FLAGS")

objc_library(
    name = "SMCalloutView",
    srcs = glob([
        "SMCalloutView/*.m",
    ]),
    hdrs = glob([
        "SMCalloutView/*.h",
    ]),

    includes = ["SMCalloutView"],

    # Force iOS compilation instead of macOS
    copts = WARNING_FLAGS["ios"] + [
        "-target", "arm64-apple-ios11.0",
    ],

    sdk_frameworks = ["UIKit"],
    sdk_includes = ["UIKit"],

    tags = ["manual"],
    visibility = [
        "//platform:__pkg__",
        "//platform/ios:__pkg__",
    ],
)
